# Copy to .env (gitignored) and set the real values before running docker compose.
MEMOIRE_DATASETS_ROOT=/home/youruser/memoire-datasets
# `id -u` on the host — lets the Airflow containers write files the host user
# can read/overwrite (bind-mounted repo), instead of the image's default uid.
AIRFLOW_UID=1000
