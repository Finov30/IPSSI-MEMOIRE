# Données — jamais dans git ; data/processed est versionné par DVC
# (data/processed.dvc, tracké, pointe vers le contenu réel)
/data-raw
/data/processed/

# Artefacts dérivés des données (régénérés par scripts/make_figures.py)
/figures/
# Sorties d'entraînement (checkpoints, history.jsonl, summary.json) — rapatriées
# à la main vers la machine du mémoire, jamais versionnées
/runs/
*.zip
*.tar
*.blend

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# Outils
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
mlruns/
.dvc/cache

# OS / éditeurs
.DS_Store
Thumbs.db
.idea/
.vscode/

# Claude Code (settings.json can be committed; local overrides never are)
.claude/settings.local.json

# Environnement local (chemins machine, jamais versionnés)
.env

# Sauvegardes de runs interrompues — jamais versionnées (checkpoints lourds)
/artefacts-interrompus/
